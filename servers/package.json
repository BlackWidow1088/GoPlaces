{
  "name": "servers",
  "version": "2.0.0",
  "description": "script for running all servers",
  "main": "sw.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "concurrently \"cd nginx && npm run start-dev && cd .. \""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.2"
  },
  "devDependencies": {
    "express": "^4.17.3"
  }
}

