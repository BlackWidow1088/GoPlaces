{
  "name": "proxy",
  "version": "2.0.0",
  "description": "proxy server for navigating to other sites",
  "main": "sw.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0"
  },
  "devDependencies": {
    "express": "^4.16.2"
  }
}
